use std::num::NonZeroU8;
 
 use crate::GenPassError;
 
 
 pub fn generate_hashes(
     _in_file: &str,
     _out_file: &str,
     _num_threads: NonZeroU8,
     _algorithm: &str,
 ) -> Result<(), GenPassError>{
 
 
 
 
     Ok(())
 }